# Ternary operator

In this example : Return value based on condition

```X++
class test
{
    public static void main(Args _args) 
    {
        int i;
        i = (4000 > 4) ? 1: 5;
        info(int2str(i));
    }
}
```
